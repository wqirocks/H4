import Sp4.Correspondence.ReturnScratch
import Sp4.Correspondence.GenericityMeasure

/-! Public import for dyadic logarithmic-radius actions, compact return, and
continuous rigidity.  The proof is kept in `ReturnScratch.lean` while the
development is being assembled, so downstream modules use this stable name. -/

namespace Sp4

end Sp4
